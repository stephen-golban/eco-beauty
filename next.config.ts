import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "images.pexels.com", "nclabeauty.com", "immaculatevegan.com", "images.quince.com"],
  },
};

export default nextConfig;
